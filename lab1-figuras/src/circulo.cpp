#include "circulo.h"

float circleArea(float r)
{
  float pi = 3.1416; // 2 * acos(0.0)
  return pi * r * r;
}

float circlePerimeter(float r)
{
  float pi = 3.1416; // 2 * acos(0.0)
  return 2 * pi * r;
}